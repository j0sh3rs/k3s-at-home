---
apiVersion: v1
kind: Service
metadata:
  name: invidious-server
  namespace: services
spec:
  selector:
    app: invidious-server
  type: ClusterIP
  ports:
    - name: web
      port: 3000
      targetPort: 3000
