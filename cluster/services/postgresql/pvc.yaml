---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-claim0
  namespace: services
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
