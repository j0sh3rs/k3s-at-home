---
kind: Service
apiVersion: v1
metadata:
  name: postgres
  namespace: services
spec:
  selector:
    app: postgres
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
