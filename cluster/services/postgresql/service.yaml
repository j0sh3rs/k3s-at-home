---
kind: Service
apiVersion: v1
metadata:
  name: postgres
  namespace: services
spec:
  selector:
    app: postgres
  type: ClusterIP
  clusterIP: None
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
