---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: whoogle
  name: whoogle
  namespace: services
spec:
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  selector:
    app: whoogle
