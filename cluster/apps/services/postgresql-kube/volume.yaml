---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-kube
  namespace: services
  labels:
    kasten-io/backup: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 50Gi
