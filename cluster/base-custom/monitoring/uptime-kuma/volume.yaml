---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-kuma-config
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteMany
  storageClass: nfs-client
  resources:
    requests:
      storage: 1Gi
